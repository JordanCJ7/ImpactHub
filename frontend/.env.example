# Frontend environment example for ImpactHub (Vite)
# Copy to frontend/.env and restart the dev server after editing.

# Backend API base URL (point to your backend during development)
VITE_API_URL=http://localhost:5000

# Frontend application URL (used for redirects if necessary)
VITE_CLIENT_URL=http://localhost:5173

# Optional: Analytics keys, feature flags, etc.
VITE_ENABLE_ANALYTICS=false
